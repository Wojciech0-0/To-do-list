<Window x:Class="ListaZadan.SortOptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ListaZadan"
        mc:Ignorable="d"
        Title="SortOptionsWindow" Height="200" Width="300" WindowStartupLocation="CenterScreen" ResizeMode="CanMinimize">
    <Grid Background="#FF42474B">
        <Grid Background="#FF868E96" Margin="0,10,0,35">
            <StackPanel Margin="1">
                <Label Content="Sortuj według:" FontSize="16" Margin="0,0,0,6"/>
                <RadioButton x:Name="rbPriority" Content="Priorytet" IsChecked="True" FontSize="14" Margin="0,5"/>
                <RadioButton x:Name="rbDueDate" Content="Data" FontSize="14" Margin="0,5"/>
                <RadioButton x:Name="rbName" Content="Nazwa" FontSize="14" Margin="0,5"/>
            </StackPanel>
        </Grid>
        <Button Content="OK" Width="80" Margin="100,138,112,1" HorizontalAlignment="Center" Click="Ok_Click"/>
    </Grid>
</Window>
